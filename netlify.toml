# Deploy Next.js (app router + middleware) ke Netlify.
# Netlify auto-install @netlify/plugin-nextjs. Tidak perlu DB (data dari JSON).
[build]
  command = "npm run build"

[build.environment]
  NODE_VERSION = "20"

[[plugins]]
  package = "@netlify/plugin-nextjs"
